# Getting started with development of DeerAnalysis










# Starting the `dash` app for development
To start the `dash` app for development, run the following command in your terminal:

```bash
python src/app.py
``` 
